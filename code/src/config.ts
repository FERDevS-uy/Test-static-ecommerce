const repositoryName = "/Test-static-ecommerce";
const url = `https://ferdevs-uy.github.io${repositoryName}/`;

const pageData = {
  site: url,
  base: repositoryName,
  pageTitle: "Test Ecommerce title",
};

export { pageData as config };
